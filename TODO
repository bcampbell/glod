skel dir should be optional
allow static files in content dir (eg images)
report multiple errors before bailing out
provide default template (for use with no templates dir)
support generation of .html urls (uglyURLs setting)
add 'where' fn to templates (based on hugo one)
emit a warning if site has no root index page
make it installable via go-get
"generated site" message is rubbish
add license
add authors
installation instructions
glod website - add minimum margins for small viewport.
x rename config.toml to site.toml

